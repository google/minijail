% minijail-config-file v0
# Comments
# enable mount namespace
ns-mount
# mounts and bind-mounts
mount = none,/,none
bind-mount = /,/
mount-dev
# Preload library.
preload-library = /fake-path
# Remount mode.
K = shared
# Single char options.
i
