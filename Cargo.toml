[package]
name = "minijail-sys"
version = "0.0.11"
authors = ["The Chromium OS Authors"]
edition = "2018"
build = "build.rs"

[lib]
path = "lib.rs"

[dependencies]
libc = "0.2.44"

[build-dependencies]
pkg-config = "0.3"
